pipeline {
   agent any

   stages {
      stage('checkout') {
         steps {
            echo 'pulling from the repository'
         }
      }
       stage('build') {
         steps {
            echo 'building application'
         }
      } 
       stage('deploy') {
         steps {
            echo 'deploying the application'
         }
      } 
   }
}
