pipeline {
   agent any

   stages {
      stage('checkout') {
         steps {
            echo 'pulling from repository'
         }
      }
       stage('build') {
         steps {
            echo 'building application'
         }
      } 
       stage('deploy') {
         steps {
            echo 'deploying application'
         }
      } 
   }
}
